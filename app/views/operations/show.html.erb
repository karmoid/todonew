<p id="notice"><%= notice %></p>

<p style="background-color: <%= @operation.cal_color %>">
  <b>Name:</b>
  <%= @operation.name %>
  (nom court: <%= @operation.short_name %> )
  
</p>

<p>
  <b>Description:</b>
  <%= @operation.description %>
</p>

<p>
  <b>Groupe d'opérations:</b>
  <%= link_to @operation.opegroup.name, @operation.opegroup %>
</p>

<%= render :partial => "shared/events", :locals => { :events => @interventions } %>

<%= link_to 'Edit', edit_operation_path(@operation) %> |
<%= link_to 'Back', operations_path %>
